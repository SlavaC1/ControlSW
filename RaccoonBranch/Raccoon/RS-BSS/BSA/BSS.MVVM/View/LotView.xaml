<UserControl x:Class="BSS.MVVM.View.LotView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:properties="clr-namespace:BSS.MVVM.Properties"
             xmlns:view="clr-namespace:BSS.MVVM.View"
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">

    <UserControl.Resources>
        <ResourceDictionary Source="Resources.xaml" />
    </UserControl.Resources>

    <Border Style="{StaticResource ViewBorder}" DataContext="{Binding CurrentMaterialInfo}"
            Visibility="{Binding ElementName=content, Path=HasContent, Converter={StaticResource BooleanToVisibilityConverter}}">
        <ContentControl Name="content" Content="{Binding}" FontSize="16" />
    </Border>

</UserControl>
